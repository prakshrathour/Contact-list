# Contact Book (Python CLI)

This is a simple command-line Contact Book application built using Python.

It allows you to store and manage contacts directly from the terminal.

## Features

- Add a contact (supports multiple numbers for same name)
- View all contacts
- Search a contact
- Delete a contact or a specific number
